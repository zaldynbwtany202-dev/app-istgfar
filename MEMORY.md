# 🧠 ذاكرة المشروع — تطبيق الاستغفار

> هذا الملف هو الذاكرة الدائمة للمشروع. يُحدَّث في كل جلسة عمل.
> آخر تحديث: 2026-09-18

## معلومات المشروع
- **الاسم:** تطبيق الاستغفار (تطبيق المستخدم الشخصي) — «التسبيح والاستغفار — LUXE».
- **الحزمة:** `com.genspark.istighfar` — الإصدار 3.0 (رمز 4)، minSdk 24، targetSdk 34.
- **البنية:** تطبيق WebView بسيط — نشاط واحد `MainActivity`، وكل الواجهة في `assets/www/index.html` (~320 ك.ب).
- **الملف المستلم:** `uploads/Istighfar.apk` (237 ك.ب) — مستلم بتاريخ 2026-09-18.
- **مزايا التطبيق الحالية:** شاشة بداية + تعريف، عدّاد «كرة نابضة»، إحصائيات (ملخص، ٧ أيام، الذكر الأكثر)، تخصيص، بيانات، وضع ليلي/نهاري، خط Amiri مدمج.
- **الهدف:** تطوير التطبيق (الميزات المطلوبة قيد التحديد مع المستخدم).

## تفضيلات المستخدم واتفاقيات العمل
1. **كل الملفات تحفظ داخل المستودع** — بما فيها الملفات المرفوعة (مجلد `uploads/`)، ولا يُستثنى شيء عبر `.gitignore`.
2. **سجل التطوير إلزامي:** كل جلسة عمل تُوثَّق في `docs/DEVLOG.md` (إنجازات، أخطاء، قرارات).
3. **الدفع إلى GitHub:** كل تغيير مهم يُرفع إلى الفرع `arena/01a0b425-app-istgfar`.
4. التوثيق والمراسلات بالعربية (اللغة الأساسية للمستخدم).

## بنية المستودع
```
├── MEMORY.md                    ← هذا الملف (ذاكرة المشروع)
├── README.md                    ← وصف المشروع
├── docs/SPEC.md                 ← مواصفات المنتج الرسمية للنسخة الجديدة
├── docs/DEVLOG.md               ← سجل التطوير الزمني
├── app/index.html               ← 🚀 التطبيق الجديد «نُور» (النسخة الحية)
├── tools/upload_server.py       ← خادم رفع الملفات (منفذ 8080)
├── tools/axml_dump.py           ← أداة فك تشفير AXML (خاصة بنا)
├── uploads/                     ← الملفات المستلمة من المستخدم
│   ├── Istighfar.apk            ← ملف التطبيق الأصلي المستلم
│   └── RECEIVED.log             ← سجل آلي لكل ملف مرفوع
├── apk_extracted/               ← محتويات الـ APK بعد الفك
│   ├── AndroidManifest.xml      ← النسخة الثنائية
│   ├── AndroidManifest_decoded.xml ← بعد فك التشفير
│   ├── assets/www/index.html    ← قلب التطبيق (الواجهة كاملة)
│   ├── classes.dex, res/, resources.arsc, META-INF/
└── (الكود المصدري المُعاد بناؤه سيُضاف هنا)
```

## آلية استلام الملفات
- خادم رفع يعمل عبر `python3 tools/upload_server.py` على المنفذ 8080 (مرتبط بـ 0.0.0.0).
- يظهر للمستخدم كمعاينة مباشرة، يرفع عبرها ملف الـ APK.
- كل رفع يُسجَّل تلقائيًا في `uploads/RECEIVED.log`.

## الحالة الراهنة
- ⛔ **قرار 2026-09-18:** إيقاف كل أعمال تعديل WeMuslim نهائيًا بعد اكتشاف أنه تطبيق تجاري لطرف ثالث (بحث الويب أكّد: +100 مليون تنزيل، المطور Metaverse Technology FZ-LLC). لا مساعدة في تجاوز حمايته أو إعادة توقيعه أو توزيع نسخ معدلة منه.
- 🧹 نُظف المستودع: حُذفت ملفات WeMuslim المرفوعة والنسخ المعدلة من الفرع ومن تاريخه.
- ✅ الباقي للعمل عليه: تطبيق الاستغفار `Istighfar.apk` (تطبيق المستخدم) + تطبيق «نُور» الأصلي المبني هنا (`app/index.html`) + بناء تطبيق شامل جديد من الصفر يملكه المستخدم بالكامل.

## خارطة الطريق المتفق عليها
1. استقبال كل تطبيقات المستخدم السابقة وتحليلها. ✅ (2 مستلمة)
2. استخلاص قائمة المزايا الموحّدة منها جميعًا. ✅ (موثقة في `analysis/`)
3. بناء التطبيق الشامل وفق **مواصفات المنتج الرسمية `docs/SPEC.md`**:
   بسيط + جذاب + احترافي + ممتع، بمحتوى مدمج بلا خادم، بدون أخبار/مراسلة/إعلانات.
4. التجميع والتوقيع وإنتاج الـ APK النهائي للاختبار (يلزم توفير Java لاحقًا).

## مواصفات النسخة الجديدة (خلاصة قرارات المستخدم 2026-09-18)
- مشاكل القديم (كلها): بطء وثقل، أعطال، إعلانات كثيرة، تعقيد وكثرة ميزات.
- عناصر عدم الملل: تحديات ومكافآت + محتوى يومي متجدد + تصميم وحركات أفضل.
- النطاق: كل الوحدات **عدا** الأخبار والمراسلة.
- الشعار: «بسيط، جذاب، احترافي، ممتع، ولا يُشعر بالملل».

## تطبيقات المستخدم المستلمة
| # | الملف | الحالة | الوصف |
|---|---|---|---|
| 1 | `uploads/Istighfar.apk` | ✅ تم التحليل | التسبيح والاستغفار — LUXE، حزمة `com.genspark.istighfar`، WebView — تطبيق المستخدم الفعلي |
| 2 | WeMuslim v3.5.2 | ⛔ **أُوقف العمل نهائيًا** | تبيّن أنه تطبيق تجاري لطرف ثالث (Metaverse Technology FZ-LLC، +100 مليون تنزيل) وليس تطبيق المستخدم. النسخة المرفوعة كانت إعادة توقيع من مواقع التوزيع. حُذفت كل ملفاته وتعديلاته من المستودع والتاريخ |

## قرارات مهمة
| التاريخ | القرار |
|---|---|
| 2026-09-18 | اعتماد `docs/DEVLOG.md` سجلًا رسميًا للتطوير و`MEMORY.md` ذاكرةً للمشروع |
| 2026-09-18 | حفظ جميع الملفات المرفوعة داخل المستودع وإلغاء استثنائها من Git |
| 2026-09-18 | مسار التطوير: تعديل `index.html` ثم إعادة تجميع وتوقيع الـ APK |
| 2026-09-18 | **الهدف الجديد:** المستخدم طوّر سابقًا عدة تطبيقات أذكار بنفسه (كل التطبيقات المرسلة ملكه)، ويريد بناء **نسخ شاملة واحترافية** منها — يمكن تحليل أكوادها وإعادة استخدامها بحرية |
| 2026-09-18 | **الشكل النهائي المتفق عليه:** تطبيق واحد شامل يضم كل شيء: أذكار وأدعية كاملة (صباح، مساء، أسماء حسنى…) + أوقات الصلاة والقبلة والقرآن + إشعارات وويدجت وأصوات وأهداف يومية |
| 2026-09-18 | **الذوق البصري للمستخدم:** رفض تصميم v0.1 (الألوان والتصميم)، واختار الطابع **العصري الجريء**: تدرجات قوية + تأثيرات زجاجية + تصميم شبابي. قاعدة مستقبلية: تجنب الألوان الهادئة/المسطحة، والأيقونات رسومات وليس إيموجي |

## أخطاء معروفة / تحذيرات
- `pip install` في الساندبوكس يتطلب `--break-system-packages` (حماية PEP 668).
- مكتبة `axmlparserpy` معطوبة على Python 3 — نستخدم أداتنا `tools/axml_dump.py` بدلًا منها.
- لا تتوفر حاليًا أدوات `aapt` / `apktool` / `jadx` مثبتة مسبقًا — يلزم تجهيز أدوات إعادة البناء والتوقيع قبل إنتاج APK جديد.

## أدوات مثبّتة في البيئة
- Python 3.11.2.
- `androguard 4.1.4` (تحليل APK/dex) + أداتنا `tools/dex_strings.py` لاستخراج سلاسل الـ dex بسرعة.
- `axmlparserpy` (مثبتة لكن معطوبة — لا تُستخدم).
- **قيود البيئة:** لا يوجد Java (تعذّر تثبيته: مستودعات Debian محجوبة وتنزيلات `*.githubusercontent.com` محجوبة)، لذا لا يتوفر `jadx`/`apktool` حاليًا. نعوّض بأدوات بايثون.
- **مجلدات عمل محلية لا تُحفظ في Git** (مولّدة وقابلة لإعادة الإنشاء): `analysis/wemuslim/dexwork/`، `analysis/wemuslim/strings_all.txt`.
