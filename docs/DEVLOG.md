# 📋 سجل التطوير — تطبيق الاستغفار

> كل ما يحدث في المشروع يُسجَّل هنا: الإنجازات ✅، الأخطاء ❌، القرارات 🔶، الملفات الواردة 📦.
> الأحدث في الأعلى.

---

## 2026-09-18 (الجلسة الأولى)

### 📦 ملفات واردة
- **استلام `uploads/Istighfar.apk`** (242,564 بايت ≈ 237 ك.ب) عبر خادم الرفع في الساعة 10:57.

### ✅ إنجازات
- تجهيز خادم رفع الملفات `tools/upload_server.py` (منفذ 8080، واجهة عربية، سحب وإفلات، شريط تقدّم، تسجيل آلي في `uploads/RECEIVED.log`).
- فك ضغط الـ APK كاملًا إلى مجلد `apk_extracted/`.
- كتابة أداة خاصة `tools/axml_dump.py` لفك تشفير ملفات AXML الثنائية (مبنية من الصفر، بدون مكتبات خارجية).
- فك تشفير `AndroidManifest.xml` بنجاح وحفظه في `apk_extracted/AndroidManifest_decoded.xml`.
- إنشاء ذاكرة المشروع `MEMORY.md` وسجل التطوير هذا `docs/DEVLOG.md`.
- إلغاء استثناءات `.gitignore` حتى يُحفظ **كل** ملف في المستودع، ودفع كل شيء إلى GitHub.

### 🔍 نتائج فحص التطبيق
| البند | القيمة |
|---|---|
| اسم الحزمة | `com.genspark.istighfar` |
| الإصدار | 3.0 (رمز الإصدار 4) |
| الحد الأدنى أندرويد | 24 (أندرويد 7) |
| الهدف | SDK 34 |
| البنية | تطبيق WebView — واجهة واحدة `MainActivity` |
| قلب التطبيق | `assets/www/index.html` (ملف واحد ~320 ك.ب: HTML + CSS + JS) |
| الخط | Amiri مدمج داخل الملف (base64) |

**المزايا الموجودة حاليًا في الواجهة:**
- شاشة بداية (Splash) + شاشة تعريف (Onboarding) من 3 شرائح.
- عدّاد تفاعلي «كرة نابضة» للتسبيح.
- إحصائيات: 📊 ملخص الإنجاز، 🗓️ نشاط آخر ٧ أيام، 📿 الذكر الأكثر تكرارًا.
- قسم التخصيص ⚙️ وقسم البيانات 🗄️ (مع خيار مسح).
- وضعان: ☀️ نهاري و🌙 ليلي.
- شبكة قوائم وأذكار.

### ❌ أخطاء ومشاكل (وطرق حلّها)
1. **`pip install androguard` فشل** — بسبب حماية النظام PEP 668.
   → الحل: استخدام `--break-system-packages`.
2. **مكتبة `axmlparserpy` معطوبة على Python 3** — تستورد وحدات بأسلوب Python 2 (`import bytecode`).
   → الحل: الاستغناء عنها وكتابة الأداة الخاصة `tools/axml_dump.py`.

### 🔶 قرارات
- بما أن التطبيق مبني على WebView، فمسار التطوير الأسلم: تعديل ملف `index.html` ثم إعادة تجميع وتوقيع الـ APK.
- اعتماد سياسة: كل ملف يُحفظ في المستودع + توثيق كل جلسة في هذا السجل.

### ⏭️ الخطوات القادمة
- [ ] استقبال تطبيقات المستخدم السابقة الباقية وتحليلها واحدًا تلو الآخر.
- [ ] تحليل كود الجافاسكربت كاملًا في `index.html` (العدّاد، التخزين، الأذكار).
- [ ] تجهيز أدوات إعادة البناء والتوقيع (apktool / zip + apksigner).

### 🔶 توجّه جديد (لاحقًا في نفس الجلسة)
- المستخدم أوضح أن **كل التطبيقات التي سيرسلها من تطويره الشخصي سابقًا**، والهدف بناء **نسخ شاملة واحترافية** منها — لا مانع من تحليل أكوادها وإعادة استخدام أي جزء منها.

---

## 2026-09-18 — بداية الجلسة
- المستودع وصل بحالته الأولى: ملف `README.md` فقط (الالتزام الأول `4e7e764`).
