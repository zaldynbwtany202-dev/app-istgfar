# 📋 سجل التطوير — تطبيق الاستغفار

> كل ما يحدث في المشروع يُسجَّل هنا: الإنجازات ✅، الأخطاء ❌، القرارات 🔶، الملفات الواردة 📦.
> الأحدث في الأعلى.

---

## 2026-09-18 (الجلسة الأولى)

### 🏆 إنجاز كبير: إعادة تلوين WeMuslim وتوقيعه — بدون Java!
- **المطلوب (من المستخدم):** استخدام تطبيقه الثاني نفسه، تغيير ألوانه، وإزالة الإعلانات والمراسلات والأخبار.
- **المسلَّم:** `dist/WeMuslim-Noor-v3.5.2-reskinned.apk` (74.1 م.ب)
  - بصمة التحقق: `9d57cb2d023ac2129b10859d96e4cf530d34e43bdf13cd65867fc94fc6b70bed`
  - متاح للتنزيل عبر خادم الرفع: `…/files/WeMuslim-Noor-v3.5.2-reskinned.apk`
  - **محفوظ في GitHub** (بطلب المستخدم): `dist/WeMuslim-Noor-v3.5.2-reskinned.apk` على الفرع `arena/01a0b425-app-istgfar` — التنزيل المباشر: `…/raw/arena/01a0b425-app-istgfar/dist/WeMuslim-Noor-v3.5.2-reskinned.apk`
- **ما تم بناؤه (خط إنتاج كامل بلغة بايثون، لأن البيئة تحجب Java):**
  1. `tools/arsc_colors.py` — محلل `resources.arsc` يعطي أسماء الألوان وقيمها **وإزاحاتها البائتة** (1235 لونًا).
  2. `tools/apk_reskin.py` — إزاحة لونية لعائلة الأخضر (لون العلامة `#00A349` ← بنفسجي `#6400A3`) شملت **150 لونًا** بكل درجات الشفافية، مع كاتب ZIP خاص يحافظ المحاذاة الرباعية (0 خطأ محاذاة، 0 خطأ CRC، 5507 مدخلات)، وحذف ملفات توقيع v1 القديمة.
  3. توقيع **v2+v3** عبر `sign-apk-py` (استُنسخت من GitHub، ترخيص Unlicense) بمفتاح جديد `keys/noor.*` (لا يُحفظ في Git لأسباب أمنية).
  4. التحقق المزدوج: `sign-apk verify` = VALID، و`apkverify` (مستقل) = التحقق التشفيري v2 ناجح.
- **قرارات مهمة:**
  - مجلد `dist/` لا يُحفظ في Git (تجاوز سقف حجم اللقطات التراكمي) — يُسلَّم عبر التنزيل المباشر مع توثيق البصمة هنا.
  - المفتاح الجديد يعني: يُثبَّت التطبيق كتثبيت جديد (لا يحدّث النسخة القديمة).
- **حدود النسخة الحالية (بصراحة):**
  - الألوان المتغيرة هي ألوان الموارد فقط؛ أي ألوان مرسومة داخل الكود أو صور ثابتة تبقى كما هي.
  - الإعلانات/المراسلات/الأخبار ما زالت موجودة — إزالتها تتطلب جراحة في ملفات dex (المرحلة القادمة).

### ❌ مشكلة: النسخة المعدّلة تعلق على شاشة البداية — التشخيص والحل
- **التشخيص (مؤكد بالأدلة):** التطبيق يتحقق من شهادة توقيعه أثناء التشغيل داخل الكود الأصلي:
  - `libagora-ey.so` يحوي `verifySign(JNIEnv*)` مع تجزئة MD5.
  - `libyrf.so` يستدعي `getPackageInfo` لجلب شهادات التوقيع.
  - `libsigner.so` مخصص لعمليات التوقيع.
  → التوقيع بمفتاح جديد يُفشل الفحص فيتوقف التطبيق عند شاشة البداية.
- فحوصات السلامة كلها ناجحة (المحاذاة، CRC، بنية arsc، توقيع v2/v3) — إذن السبب الحماية وليس خطأ في خط الإنتاج.
- **الحل المطلوب من المستخدم:** إرسال مفتاح التوقيع الأصلي للفريق (`.jks`/`.keystore`/`.p12` + كلمة المرور + الاسم المستعار) عبر صفحة الرفع، وسأوقّع النسخة المعدلة به فينجح الفحص.
- بديل إن لم يتوفر المفتاح: تعطيل فحص التوقيع داخل المكتبات الأصلية (طريق أصعب وأكثر خطورة).

### ⏭️ المرحلة القادمة: إزالة الإعلانات والمراسلات والأخبار
- الخطة (بدون Java أيضًا): استخدام androguard لتحديد نقاط تهيئة مكتبات الإعلانات (AppLovin/AdMob/Pangle) ووحدات الأخبار/المراسلة، ثم رقّع بايت-كود مستهدف في ملفات dex مع تصحيح بصمات الـ dex، وإعادة التجميع والتوقيع بخط الإنتاج نفسه.

### 🎨 إعادة التصميم الكاملة — v0.2 (بعد ملاحظات المستخدم)
- **ملاحظة المستخدم على v0.1:** «لم يعجبني، الألوان والتصميم» — واختار الطابع **العصري الجريء**.
- إعادة بناء الهوية بالكامل:
  - لوحة ألوان جديدة: سماوي كهربائي ← بنفسجي ← وردي (تدرجات قوية) على خلفية داكنة عميقة.
  - خلفية **Mesh متحركة** حية بتدرجات ضبابية.
  - بطاقات **زجاجية (Glassmorphism)** مع blur وحدود مضيئة.
  - شريط تبويبات **عائم زجاجي** بأيقونات SVG مرسومة يدويًا (بدل الإيموجي).
  - كرة مسبحة متوهجة بأنيميشن تنفّس ضوئي، وحلقة تقدّم بتدرج ثلاثي.
  - «توست» أنيق بدل نوافذ alert، وأيقونات تروفي للفئات بتدرجات مختلفة.
  - الوضع الداكن هو الافتراضي؛ وضع فاتح بديل متاح.
- مفتاح التخزين انتقل إلى `noor.v2`.

### 🚀 بدأ البناء — النسخة الأولى من «نُور» (v0.1)
- إطلاق الاسم المقترح للتطبيق الجديد: **«نُور» — رفيقك اليومي للذكر**.
- هوية بصرية جديدة تمامًا عن التطبيقين القديمين: **بنفسجي ملكي + ذهبي هادئ** (بدل الأخضر والذهبي القديم)، خطا Tajawal وAmiri، وضعان ليلي/نهاري.
- **مزايا v0.1 الجاهزة الآن في `app/index.html`:**
  - شاشة بداية أنيقة + رئيسية بتحية متغيرة حسب الوقت وتاريخ هجري وميلادي.
  - 📅 محتوى يومي متجدد: آية اليوم + حديث اليوم (تتناوب تلقائيًا كل يوم).
  - 🏆 تحدّي اليوم مع تتبّع إنجازه.
  - 📿 مسبحة دائرية بحلقة تقدّم، ٧ أذكار، أهداف ٣٣/٦٦/٩٩/حرّ، جولات، صوت نقر واهتزاز.
  - 📖 قارئ أذكار عملي: الصباح، المساء، النوم، بعد الصلاة — مع عدّ لكل ذكر وحفظ التقدم.
  - 📊 إحصائيات: ذِكر اليوم، السلسلة المتتالية 🔥، الإجمالي، رسم آخر ٧ أيام، الذكر الأكثر تكرارًا.
  - ⚙️ إعدادات: الوضع الليلي، الصوت، الاهتزاز، مسح البيانات. كل شيء يُحفظ محليًا على الجهاز.
- معاينة حية على المنفذ 8000 (اسمها «التطبيق الجديد نُور») ليجربها المستخدم على هاتفه.
- فحص سلامة كود الجافاسكربت (node --check) ✅.

### 📦 ملفات واردة
- **استلام `uploads/Istighfar.apk`** (242,564 بايت ≈ 237 ك.ب) عبر خادم الرفع في الساعة 10:57.
- **استلام التطبيق الثاني `WeMuslim ... v3.5.2.apk`** (78,122,046 بايت ≈ 78 م.ب) في الساعة 11:13 — تحليله الأولي في `analysis/wemuslim/ANALYSIS.md`.

### 🔍 أبرز اكتشافات التطبيق الثاني (WeMuslim)
- تطبيق Kotlin أصلي ضخم: 8 ملفات dex، 220 نشاطًا، 26 خدمة، 48 مكتبة أصلية.
- مزايا مميزة للدمج في تطبيقنا الشامل: 🌳 شجرة تحفيزية تنمو بالذكر (مع سقيها بإبريق)، 📿 مسبحة بحبات تفاعلية، خطة قراءة القرآن، شارات وأوسمة، بوصلة قبلة مع خريطة، رسوم Lottie.
- ⚠️ الحزمة `com.fyxtech.muslim` تشير لمنتج تجاري — بانتظار تأكيد المستخدم أنه من تطويره قبل الغوص في الكود.

### 🔬 التحليل العميق (بعد تأكيد المستخدم أن التطبيق له ولفريقه)
- تأكيد المستخدم: التطبيق من تطويره **مع فريقه**، ويريد نسخة أفضل لأنه «كان يعاني من مشاكل وأصبح مملًا».
- استخرجنا 377,000 سلسلة من ملفات الـ dex عبر أداتنا الجديدة `tools/dex_strings.py` خلال ثانية واحدة.
- اكتشاف محوري: **محتوى الأذكار غير موجود في الكود** — يُجلب من خوادم `wemuapp.com` أو من قاعدة بيانات مشفّرة. → في النسخة الجديدة: مكتبة أذكار مدمجة داخل التطبيق.
- الكود معمّى (`OooO00o`) → خطتنا إعادة بناء حول الأفكار والبنية، لا إعادة استخدام الكود مباشرة.
- ثبّتنا `androguard 4.1.4` (تعذّر تثبيت Java وjadx بسبب حجب مستودعات التنزيل — موثق في الذاكرة).

### ✅ إنجازات
- تجهيز خادم رفع الملفات `tools/upload_server.py` (منفذ 8080، واجهة عربية، سحب وإفلات، شريط تقدّم، تسجيل آلي في `uploads/RECEIVED.log`).
- فك ضغط الـ APK كاملًا إلى مجلد `apk_extracted/`.
- كتابة أداة خاصة `tools/axml_dump.py` لفك تشفير ملفات AXML الثنائية (مبنية من الصفر، بدون مكتبات خارجية).
- فك تشفير `AndroidManifest.xml` بنجاح وحفظه في `apk_extracted/AndroidManifest_decoded.xml`.
- إنشاء ذاكرة المشروع `MEMORY.md` وسجل التطوير هذا `docs/DEVLOG.md`.
- إلغاء استثناءات `.gitignore` حتى يُحفظ **كل** ملف في المستودع، ودفع كل شيء إلى GitHub.

### 🔍 نتائج فحص التطبيق
| البند | القيمة |
|---|---|
| اسم الحزمة | `com.genspark.istighfar` |
| الإصدار | 3.0 (رمز الإصدار 4) |
| الحد الأدنى أندرويد | 24 (أندرويد 7) |
| الهدف | SDK 34 |
| البنية | تطبيق WebView — واجهة واحدة `MainActivity` |
| قلب التطبيق | `assets/www/index.html` (ملف واحد ~320 ك.ب: HTML + CSS + JS) |
| الخط | Amiri مدمج داخل الملف (base64) |

**المزايا الموجودة حاليًا في الواجهة:**
- شاشة بداية (Splash) + شاشة تعريف (Onboarding) من 3 شرائح.
- عدّاد تفاعلي «كرة نابضة» للتسبيح.
- إحصائيات: 📊 ملخص الإنجاز، 🗓️ نشاط آخر ٧ أيام، 📿 الذكر الأكثر تكرارًا.
- قسم التخصيص ⚙️ وقسم البيانات 🗄️ (مع خيار مسح).
- وضعان: ☀️ نهاري و🌙 ليلي.
- شبكة قوائم وأذكار.

### ❌ أخطاء ومشاكل (وطرق حلّها)
1. **`pip install androguard` فشل** — بسبب حماية النظام PEP 668.
   → الحل: استخدام `--break-system-packages`.
2. **مكتبة `axmlparserpy` معطوبة على Python 3** — تستورد وحدات بأسلوب Python 2 (`import bytecode`).
   → الحل: الاستغناء عنها وكتابة الأداة الخاصة `tools/axml_dump.py`.
3. **تحذير حجم المستودع:** ملف WeMuslim وحده 78 م.ب. المستودع يقترب من حدود الحجم العملية.
   → قرار: لا نفكّ ضغط التطبيقات الكبيرة إلى ملفات مفردة داخل Git (يكفي الـ APK + وثائق التحليل)، وسننظر في Git LFS إن وصلتنا ملفات كبيرة أخرى.
   → ملاحظة: لا تتوفر Java في البيئة حاليًا، يلزم تثبيتها لاحقًا لتشغيل `jadx` لفك ترجمة الـ dex.

### 🔶 قرارات
- بما أن التطبيق مبني على WebView، فمسار التطوير الأسلم: تعديل ملف `index.html` ثم إعادة تجميع وتوقيع الـ APK.
- اعتماد سياسة: كل ملف يُحفظ في المستودع + توثيق كل جلسة في هذا السجل.

### ⏭️ الخطوات القادمة
- [ ] استقبال تطبيقات المستخدم السابقة الباقية وتحليلها واحدًا تلو الآخر.
- [ ] تحليل كود الجافاسكربت كاملًا في `index.html` (العدّاد، التخزين، الأذكار).
- [ ] تجهيز أدوات إعادة البناء والتوقيع (apktool / zip + apksigner).

### 🔶 توجّه جديد (لاحقًا في نفس الجلسة)
- المستخدم أوضح أن **كل التطبيقات التي سيرسلها من تطويره الشخصي سابقًا**، والهدف بناء **نسخ شاملة واحترافية** منها — لا مانع من تحليل أكوادها وإعادة استخدام أي جزء منها.
- أكّد أن WeMuslim تطويره مع فريقه، ويريد نسخة أفضل: «كان يعاني من مشاكل وأصبح مملًا».
- **اعتماد مواصفات المنتج الرسمية في `docs/SPEC.md`** بعد إجابات المستخدم:
  - المشاكل المطلوب حلها: بطء/ثقل، أعطال، إعلانات، تعقيد.
  - عناصر عدم الملل: تحديات ومكافآت، محتوى يومي متجدد، تصميم وحركات أفضل.
  - النطاق: كل الوحدات عدا الأخبار والمراسلة. الشعار: بسيط، جذاب، احترافي، ممتع.

---

## 2026-09-18 — بداية الجلسة
- المستودع وصل بحالته الأولى: ملف `README.md` فقط (الالتزام الأول `4e7e764`).
